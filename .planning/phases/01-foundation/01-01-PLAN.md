---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - .env.example
  - .gitignore
  - sentinel/__init__.py
  - sentinel/schemas/__init__.py
  - sentinel/agents/__init__.py
  - sentinel/api/__init__.py
  - sentinel/memory/__init__.py
  - sentinel/gate/__init__.py
  - sentinel/fixtures/__init__.py
  - tests/__init__.py
  - tests/conftest.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-05

must_haves:
  truths:
    - "`import sentinel` succeeds and returns version 0.1.0"
    - "`import aerospike` succeeds (wheel installed correctly for platform)"
    - "All dependency imports resolve: fastapi, anthropic, pydantic, aerospike, RestrictedPython"
    - ".env.example documents all 7 required environment variables"
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project configuration with all deps"
      contains: "sentinel"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "ANTHROPIC_API_KEY"
    - path: "sentinel/__init__.py"
      provides: "Package root with version"
      contains: "__version__"
    - path: "tests/conftest.py"
      provides: "Shared test configuration"
  key_links:
    - from: "pyproject.toml"
      to: "sentinel/__init__.py"
      via: "package definition"
      pattern: 'name = "sentinel"'
---

<objective>
Initialize the Python project structure with all dependencies, environment config, and empty package stubs.

Purpose: Every downstream plan depends on the installed package and importable module structure. Getting dependencies installed and verified (especially the aerospike C-extension wheel) prevents late discovery of build failures.
Output: Working Python package `sentinel/` with installed venv, all dependencies resolving, and test infrastructure ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project config, directory structure, and install dependencies</name>
  <files>
    pyproject.toml,
    requirements.txt,
    .env.example,
    .gitignore,
    sentinel/__init__.py,
    sentinel/schemas/__init__.py,
    sentinel/agents/__init__.py,
    sentinel/api/__init__.py,
    sentinel/memory/__init__.py,
    sentinel/gate/__init__.py,
    sentinel/fixtures/__init__.py,
    tests/__init__.py,
    tests/conftest.py
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    .planning/phases/01-foundation/01-CONTEXT.md,
    CLAUDE.md
  </read_first>
  <action>
    Per D-04 and D-05:

    1. Create `pyproject.toml` with:
       - `[project]` name="sentinel", requires-python=">=3.11"
       - Dependencies: `fastapi==0.115.14`, `uvicorn[standard]>=0.34,<0.35`, `anthropic[aiohttp]==0.86.0`, `aerospike==19.1.0`, `RestrictedPython==8.1`, `pydantic>=2.12,<3`, `python-dotenv>=1.0,<2`
       - Dev dependencies group: `pytest`, `pytest-asyncio`, `httpx`, `Pillow`
       - `[tool.pytest.ini_options]` asyncio_mode="auto"

    2. Create `requirements.txt` with pinned versions matching pyproject.toml (for environments that prefer pip -r).

    3. Create `.env.example` with these exact lines:
       ```
       ANTHROPIC_API_KEY=sk-ant-...
       AEROSPIKE_HOST=localhost
       AEROSPIKE_PORT=3000
       AEROSPIKE_NAMESPACE=sentinel
       BLAND_API_KEY=
       OKTA_DOMAIN=
       OKTA_CLIENT_ID=
       ```

    4. Create `.gitignore` with: `.venv/`, `__pycache__/`, `.env`, `*.pyc`, `.pytest_cache/`, `node_modules/`, `dist/`, `.DS_Store`

    5. Create directory structure with empty `__init__.py` in each:
       - `sentinel/__init__.py` (with `__version__ = "0.1.0"`)
       - `sentinel/schemas/__init__.py` (empty -- populated by Plan 05)
       - `sentinel/agents/__init__.py`
       - `sentinel/api/__init__.py`
       - `sentinel/memory/__init__.py`
       - `sentinel/gate/__init__.py`
       - `sentinel/fixtures/__init__.py` (empty -- populated by Plan 04)
       - `tests/__init__.py`

    6. Create `tests/conftest.py` with:
       - `import os; os.environ.setdefault("AEROSPIKE_HOST", "localhost")` etc. for test defaults
       - A `@pytest.fixture` named `fixture_data` that returns a mock FixtureData dict (placeholder until Plan 04)

    7. Create the Python 3.12 venv and install all deps:
       ```bash
       python3.12 -m venv .venv
       source .venv/bin/activate
       pip install -e ".[dev]"
       ```
       If `python3.12` not found, try `python3.11`. If aerospike wheel fails, set `ARCHFLAGS="-arch arm64"` and retry.
  </action>
  <verify>
    <automated>cd /Users/stevenyang/Documents/deep-agents-hackathon && source .venv/bin/activate && python -c "import sentinel; print(sentinel.__version__)" && python -c "import fastapi, anthropic, pydantic; print('deps ok')" && python -c "import aerospike; print('aerospike ok')" && python -m pytest tests/ -x -q --co 2>/dev/null; echo "structure ok"</automated>
  </verify>
  <done>Python package installs, all dependencies resolve including aerospike C-extension, project structure matches D-04/D-05 layout</done>
</task>

</tasks>

<verification>
```bash
# Package imports
python -c "import sentinel; print(sentinel.__version__)"

# All key deps import
python -c "import fastapi, anthropic, pydantic, aerospike; print('all deps ok')"

# Env example complete
grep -c "=" .env.example  # should be 7
```
</verification>

<success_criteria>
- `import sentinel` succeeds with __version__ = "0.1.0"
- `import aerospike` succeeds (C-extension wheel built correctly)
- `import fastapi, anthropic, pydantic` all succeed
- .env.example has all 7 env vars
- pyproject.toml has all pinned dependencies
- tests/conftest.py exists with test defaults
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
