---
phase: quick
plan: 260326-2j3
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ForensicScanPanel.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The warning overlay in the Lightbox does not cover the bottom of the invoice image"
    - "The thumbnail warning badge does not cover the bottom of the forensic scan thumbnail"
  artifacts:
    - path: frontend/src/components/ForensicScanPanel.jsx
      provides: "Forensic overlay positioned at top"
  key_links:
    - from: "Lightbox isForensic overlay"
      to: "top of image container"
      via: "style={{ top: 0, height: '8%' }}"
      pattern: "top: 0"
---

<objective>
Move the forensic warning overlays from the bottom to the top of their containers so the hidden injection text at the bottom of `invoice_forensic.png` is fully visible when expanded and in thumbnail view.

Purpose: The overlays currently sit on top of the white-on-white hidden text that the forensic scan is supposed to reveal — defeating the demo's purpose.
Output: Updated `ForensicScanPanel.jsx` with both overlays positioned at the top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/components/ForensicScanPanel.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move forensic overlays to top</name>
  <files>frontend/src/components/ForensicScanPanel.jsx</files>
  <action>
    Two targeted edits in ForensicScanPanel.jsx:

    1. **Lightbox overlay** (line 71-79): Change `style={{ bottom: '4%', height: '8%' }}` to `style={{ top: 0, height: '8%' }}`. Add `rounded-t` class to the div (remove any bottom-rounding). Update the label text to `[FORENSIC SCAN] ⚠ Hidden prompt injection — white text on white background` to make clear this is a mode indicator, not just a warning at the bottom.

    2. **Thumbnail overlay** (line 160): Change `absolute bottom-2` to `absolute top-2` on the thumbnail warning div.

    These are the only two changes needed. Do not modify any other logic, state, or structure.
  </action>
  <verify>
    Grep confirms the changes:
    `grep -n "top:" frontend/src/components/ForensicScanPanel.jsx` shows `top: 0` in Lightbox overlay
    `grep -n "top-2" frontend/src/components/ForensicScanPanel.jsx` shows `absolute top-2` in thumbnail
    `grep -n "bottom" frontend/src/components/ForensicScanPanel.jsx` shows no remaining `bottom-2` or `bottom: '4%'` references in overlay divs
  </verify>
  <done>Both overlays are anchored to the top of their containers; no overlay references `bottom` for positioning.</done>
</task>

</tasks>

<verification>
- `grep -n "bottom: '4%'" frontend/src/components/ForensicScanPanel.jsx` returns no matches
- `grep -n "absolute bottom-2" frontend/src/components/ForensicScanPanel.jsx` returns no matches
- File compiles cleanly: `cd frontend && npm run build 2>&1 | tail -5` exits 0
</verification>

<success_criteria>
Forensic Lightbox overlay sits flush at the top of the image (top: 0). Thumbnail warning badge sits at top-2. The full bottom portion of invoice_forensic.png is unobstructed in both views.
</success_criteria>

<output>
After completion, create `.planning/quick/260326-2j3-fix-forensic-scan-overlay-covering-hidde/260326-2j3-SUMMARY.md`
</output>
