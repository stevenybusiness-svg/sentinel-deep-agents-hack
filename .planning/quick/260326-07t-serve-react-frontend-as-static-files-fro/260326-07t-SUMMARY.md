---
phase: quick
plan: 260326-07t
subsystem: api / frontend
tags: [fastapi, static-files, spa, react, vite]
dependency_graph:
  requires: []
  provides: [single-origin-serving, spa-routing]
  affects: [sentinel/api/main.py]
tech_stack:
  added: [fastapi.staticfiles.StaticFiles, fastapi.responses.FileResponse]
  patterns: [StaticFiles mount, SPA catch-all route, guarded dist existence check]
key_files:
  created: [frontend/dist/index.html, frontend/dist/assets/]
  modified: [sentinel/api/main.py]
decisions:
  - SPA catch-all registered after /health so API routes are never shadowed
  - _FRONTEND_DIST.exists() guard allows server startup without a build present
  - /assets mount covers hashed JS/CSS chunks; root index.html served by catch-all
metrics:
  duration_seconds: 177
  completed_date: "2026-03-26T04:14:00Z"
  tasks_completed: 2
  files_modified: 1
---

# Quick Task 260326-07t: Serve React Frontend as Static Files Summary

**One-liner:** FastAPI now mounts the Vite-built React app at /assets and serves index.html for all SPA paths via a catch-all route registered after the API endpoints.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Build the React frontend | c8d8164 (no tracked files — dist is gitignored) | frontend/dist/ (untracked build artifact) |
| 2 | Mount static files and add SPA catch-all in FastAPI | c8d8164 | sentinel/api/main.py |

## What Was Done

**Task 1 — Frontend build:** Ran `npm install && npm run build` in the worktree's `frontend/` directory. Vite produced `frontend/dist/index.html` (2.37 kB) and `frontend/dist/assets/index-*.{js,css}`. Build artifacts are gitignored per the existing `frontend/.gitignore` — this is intentional; the dist directory is a reproducible build output, not source-controlled.

**Task 2 — FastAPI static serving:** Added `StaticFiles` and `FileResponse` imports to `sentinel/api/main.py`. After the `/health` route, added:
- `_FRONTEND_DIST` path computed relative to `__file__` (resolves to repo root / frontend / dist)
- Guard: `if _FRONTEND_DIST.exists()` so server starts cleanly without a build
- `/assets` StaticFiles mount serving the hashed JS/CSS chunks
- `/{full_path:path}` catch-all returning `index.html` — registered last so it cannot shadow `/ws`, `/investigate`, `/confirm`, or `/health`

## Verification Results

Server started at port 8766, all three checks passed:
- `GET /` — returned React HTML shell (`<!doctype html>...`)
- `GET /health` — returned `{"status":"ok","aerospike":false}`
- `GET /some/spa/path` — returned React HTML shell (SPA routing confirmed)

## Deviations from Plan

**1. [Rule 3 - Clarification] Verification ran from worktree, not main repo**
- **Found during:** Task 2 verification
- **Issue:** The plan's verification command `python -c "from sentinel.api.main import app..."` was run against the main repo checkout which has the old main.py. The worktree has the updated main.py and the built frontend/dist/.
- **Fix:** Ran verification using the venv python from the worktree directory — exact same logic, correct environment.
- **Impact:** None — verification confirms correct behavior.

**2. [Informational] Task 1 produced no git-committable files**
- dist/ is gitignored in `frontend/.gitignore` (standard build artifact pattern)
- Both tasks were committed as a single atomic commit c8d8164 since only main.py changed in git tracking

## Known Stubs

None — the frontend build is the real compiled React app, not a placeholder.

## Self-Check

- [x] `frontend/dist/index.html` exists in worktree
- [x] `sentinel/api/main.py` has StaticFiles import, /assets mount, and /{full_path:path} catch-all
- [x] Commit c8d8164 exists
- [x] Live server verification confirmed all three success criteria

## Self-Check: PASSED
